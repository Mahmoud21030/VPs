# Setup

Create a Windows QCOW2 base image with VirtIO storage, VirtIO networking, UEFI boot support, RDP enabled, and required software installed. Upload it as `windows-vm/base.qcow2`.

Configure GitHub secrets and run `runtime.yml`. Use the workflow inputs to set CPU and memory.
